syntax = "proto3";

option go_package = ".;evomorph";

// Structure to identify a job.
message Simulation {
  uint32 id = 1;
  string name = 2;
}

// Signal between services.
message Status {
  enum StatusType {
    OK = 0;
    ERROR = 1;
  }
  StatusType status = 1;
}
